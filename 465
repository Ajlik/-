#include <iostream>
using namespace std;

int main()
{
    setlocale(LC_ALL, "RU");
int res;
int mass[1000];
int a,b;
cout<< "только отрицательные числа ";
cin >>a;
cout<< "только отрицательные числа ";
cin >>b; 
 int f;
 int i;
  for(int i = a; i<b;  i++) 
      
     { 
        res= i % 2 ;
        if (res==0, i<0)
        if (res==0)
        cout<< i << ", ";
     
     
        
    }
    
    return 0;
}
